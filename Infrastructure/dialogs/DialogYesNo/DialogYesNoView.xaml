<ContentControl x:Class="recipe.Infrastructure.dialogs.DialogYesNo.DialogYesNoView"  
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                xmlns:vm="clr-namespace:recipe.Infrastructure.dialogs.DialogYesNo" 
                d:DataContext="{d:DesignInstance Type = vm:DialogYesNoViewModel}"
                mc:Ignorable="d"  
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid Margin="4">
        <Label Content="{Binding Message}"/>
        <Button Content="Да" HorizontalAlignment="Left" Margin="135,70,0,0" VerticalAlignment="Top" Width="75"   
        Command="{Binding YesCommand}"   
        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        <Button Content="Нет" HorizontalAlignment="Left" Margin="215,70,0,0" VerticalAlignment="Top" Width="75"   
        Command="{Binding NoCommand}"  
        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </Grid>
</ContentControl> 
